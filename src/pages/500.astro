---
import Layout from '../layouts/Layout.astro';
import SearchBox from '../components/SearchBox.astro';
import Flag from '../components/Flag.astro';
import { browsePath, countryPath, homePath, contactPath } from '../lib/urls';
import { countriesFor, loadSite } from '../lib/site';
import { localizeCountry } from '../lib/localize';
import { SITE_NAME } from '../lib/seo';
import { t } from '../i18n/ui';

interface Props {
  error?: unknown;
}

const strings = t('en');
const site = await loadSite();
const countries = countriesFor(site, 'en').map((c) => ({
  href: countryPath(c, 'en'),
  ...localizeCountry(c, 'en'),
}));

const title = `500 ? Server Error | ${SITE_NAME}`;
const description =
  'An unexpected server error occurred on Slang Words Online. Please refresh the page or return to the dictionary homepage.';
---

<Layout title={title} description={description} locale="en" noIndex>
  <div class="container-site py-section">
    <div class="max-w-2xl">
      <span class="inline-block rounded-full bg-surface-card px-3 py-1 text-caption font-semibold uppercase tracking-wider text-error">
        500 &bull; Server Error
      </span>
      <h1 class="mt-3 text-display-lg text-ink">Something went wrong</h1>
      <p class="mt-5 text-body-md text-body leading-relaxed">
        Our servers encountered an unexpected issue while assembling this page. This is usually temporary. You can try refreshing the page, searching for a term, or browsing from our home page.
      </p>

      <div class="mt-6 flex flex-wrap items-center gap-3">
        <button
          type="button"
          id="reload-btn"
          class="focus-ring inline-flex items-center gap-2 rounded-md bg-ink px-4 py-2.5 text-body-sm font-semibold text-canvas hover:bg-charcoal transition-colors cursor-pointer"
        >
          <svg viewBox="0 0 24 24" class="size-4" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M21 12a9 9 0 0 0-9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"></path>
            <path d="M3 3v5h5"></path>
            <path d="M3 12a9 9 0 0 0 9 9 9.75 9.75 0 0 0 6.74-2.74L21 16"></path>
            <path d="M16 21h5v-5"></path>
          </svg>
          Reload Page
        </button>

        <a
          href={homePath('en')}
          class="focus-ring inline-flex items-center gap-2 rounded-md border border-hairline bg-canvas px-4 py-2.5 text-body-sm font-semibold text-ink hover:bg-surface-card transition-colors"
        >
          Homepage
        </a>

        <a
          href={browsePath('en')}
          class="focus-ring inline-flex items-center gap-2 rounded-md border border-hairline bg-canvas px-4 py-2.5 text-body-sm font-semibold text-ink hover:bg-surface-card transition-colors"
        >
          Browse A&ndash;Z
        </a>

        <a
          href={contactPath('en')}
          class="focus-ring inline-flex items-center gap-2 rounded-md border border-hairline bg-canvas px-4 py-2.5 text-body-sm font-semibold text-ink hover:bg-surface-card transition-colors"
        >
          Report Error
        </a>
      </div>

      <div class="mt-8 border-t border-hairline pt-6">
        <h2 class="text-heading-md text-ink">Search the dictionary</h2>
        <p class="mt-1 text-body-sm text-mute">
          Looking for a specific slang term, idiom, or expression? Search directly below:
        </p>
        <div class="mt-4">
          <SearchBox autofocus locale="en" />
        </div>
      </div>
    </div>

    <nav aria-labelledby="pick-country" class="mt-section border-t border-hairline pt-8">
      <h2 id="pick-country" class="text-heading-md text-ink">{strings.orStartFromCountry}</h2>
      <ul class="mt-4 flex flex-wrap gap-2">
        {
          countries.map((c) => (
            <li>
              <a
                href={c.href}
                class="focus-ring inline-flex items-center gap-2 rounded-full bg-surface-card px-4 py-2 text-body-sm font-semibold text-ink hover:bg-surface-sunk transition-colors"
              >
                <Flag code={c.flagCode} class="h-[0.9em] w-auto shrink-0 rounded-[2px]" />
                {c.adjective}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</Layout>

<script is:inline>
  (function () {
    var reloadBtn = document.getElementById('reload-btn');
    if (reloadBtn) {
      reloadBtn.addEventListener('click', function () {
        window.location.reload();
      });
    }
  })();
</script>
